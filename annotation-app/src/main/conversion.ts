/**
 * Converts an annotation generated with the annotation software
 * to a list of thresholds.
 *
 * @param annotation
 */
function extractThresholdsFromAnnotation(annotation: JSON) {
  console.log(annotation);
}
